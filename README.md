# React_study

Created with CodeSandbox

・ULtag 周りは差分更新のほうが良いので、react で実装することを意識した仕掛けにする。
・map 等を使用してループしたレンダリングを実装する場合は key=を最初の DOM に追記すること。これは差分更新なので、何番目の差分を更新しているのかを確認できるようにするために使用するらしい、、。
設定しないとコンソールでエラーが出力される。
